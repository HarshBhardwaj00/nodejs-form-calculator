# Pure Node.js Calculator

This project is a basic calculator application built using **pure Node.js** without any external frameworks like Express.

## 🚀 Features
- Built using Node.js `http` module
- Handles POST requests manually
- Uses streams (`req.on('data')`, `req.on('end')`)
- Parses form data using `Buffer` and `URLSearchParams`
- Clean routing logic
- Beginner-friendly project for learning core Node.js

## 🛠 Technologies Used
- Node.js
- HTTP Module
- Streams & Buffers

## 📌 Learning Outcomes
- Understanding how Node.js handles HTTP requests
- Working with request streams and chunks
- Manual parsing of form data
- Server-side rendering using plain HTML

## ▶️ How to Run
```bash
node app.js
